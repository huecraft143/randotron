{
  "name": "randotron",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Randotron — a decision helper powered by genuine quantum randomness (QRNG API).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.3",
    "@vitejs/plugin-react": "^4.4.0",
    "vite": "^6.0.0",
    "wrangler": "^4.105.0"
  }
}
